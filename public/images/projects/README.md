# Project visuals

Work cards no longer require screenshots.

Each project shows a **Before → After** impact panel plus a **system flow** diagram.
Edit those in `src/data/portfolio.ts` under `beforeAfter` and `architecture`.

Optional screenshots can still be added later if you want UI images.
